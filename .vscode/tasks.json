{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "setupenv",
            "command": "source",
            "args": ["${workspaceFolder}/startup.sh"],
            "type": "shell"
        }
    ]
}
